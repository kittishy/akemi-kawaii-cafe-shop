
import { Layout } from "@/components/layout/Layout";
import { ProductDetail as ProductDetailComponent } from "@/components/shop/ProductDetail";

const ProductDetail = () => {
  return (
    <Layout>
      <ProductDetailComponent />
    </Layout>
  );
};

export default ProductDetail;
